[build]
cmd = "pip install -r requirements.txt && python manage.py collectstatic --noinput"

[start]
cmd = "python manage.py migrate && python manage.py loaddata catalog/fixtures/initial_data.json && gunicorn pmcell.wsgi:application"

[variables]
NIXPACKS_PYTHON_VERSION = "3.11"